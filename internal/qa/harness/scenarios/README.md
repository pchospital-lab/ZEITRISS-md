# Scenarios — Placeholder

Scenarios (`PHASES`, `SCENARIOS`, `PHASE2C_SCENARIO` etc.) sind aktuell **inline** in den
Harness-Scripts definiert (siehe `harness/group-harness.py`, `harness/coreops-harness.py`).

**Geplante Extraktion (offene Task):** YAML/JSON-Scenario-Files hier ablegen,
Harness lädt sie über `--scenario <name>`-CLI-Arg. Bis dahin: diesen Ordner
leer lassen oder Scenario-Skizzen als `.md`-Notizen hier parken.
