#!/usr/bin/env bash
set -euo pipefail

export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8

CHANGED=$(git diff --cached --name-only --diff-filter=ACM | tr '\n' ' ')
[ -z "$CHANGED" ] && exit 0

python3 .hooks/normalize_check.py $CHANGED
